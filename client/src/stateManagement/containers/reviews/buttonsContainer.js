import { connect } from 'react-redux'
import Buttons from '../../../components/reviews/buttons.jsx'

const mapDispatchToProps = {
  action: ''
}

export default connect(null, mapDispatchToProps)(Buttons)
